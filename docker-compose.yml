services:
  api:
    build: .
    container_name: api
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data

  app_logs:
    build: .
    container_name: app_logs
    command: ["python", "log_app.py"]
    depends_on:
      - api